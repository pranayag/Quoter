import React, { Component } from 'react';

class Random extends Component {
    state = {  }

    render() { 
        return ( 
            <div>
                <button onClick = {this.Search.render}>
                    Random
                </button>
            </div>
         );
    }
}
 
export default Random;